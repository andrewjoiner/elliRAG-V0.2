import RagControls from "../../../src/components/chat/RagControls";

export default function AnalyticsPage() {
  // Provide a no-op settings change handler as default
  return <RagControls onSettingsChange={() => {}} />;
}